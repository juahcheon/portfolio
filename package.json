{
  "name": "portfolio-root",
  "private": true,
  "scripts": {
    "dev:web": "npm run dev --prefix web",
    "dev:api": "npm run dev --prefix api",
    "verify:api": "npm run verify --prefix api",
    "verify:web": "npm run verify --prefix web",
    "verify": "npm run verify:api && npm run verify:web"
  }
}
